"""EVGA Z12 keyboard configuration library (macOS/Linux).

Reads and writes EVGA Z12 onboard settings via HID feature reports.
All protocol constants are verified against on-device captures — see
docs/research.md.
"""
